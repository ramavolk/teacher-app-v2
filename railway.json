{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "npm install --legacy-peer-deps --no-package-lock && npm run build"
    },
    "deploy": {
      "startCommand": "npm start",
      "restartPolicyType": "ON_FAILURE"
    }
  }